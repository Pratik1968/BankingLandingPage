import { ReactNode } from "react";

export default function About({className}:{className:string}):ReactNode{

    return(
        <div className={className}>
            
        <p>About</p>
        </div>
    );
}