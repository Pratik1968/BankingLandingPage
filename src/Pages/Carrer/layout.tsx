import { ReactNode } from "react";

export default function Carrer({className}:{className:string}):ReactNode{

    return(
        <div className={className}>

        <p>Carrer</p>
        </div>
    );
}